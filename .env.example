# 复制本文件为 .env.local，然后按需填写。
# 注意：当前项目前端会通过 Vite 在构建时注入 OPENAI_* 到浏览器端（仅用于测试/自用，部署时请谨慎）。

# ========== AI 祝福（可选：不填则使用离线兜底文案） ==========
# OpenAI 兼容接口的 Base URL（会自动补全到 /v1/chat/completions）
OPENAI_BASE_URL=https://api.openai.com

# API Key（不要提交到 git）
OPENAI_API_KEY=

# 模型 ID（示例：gpt-4o-mini；也可填写你平台支持的任意模型）
OPENAI_MODEL=

# ========== 数据库（可选：不填则本地 API 进入内存模式） ==========
# MySQL 连接串（注意特殊字符需要 URL 编码）
# 格式：mysql://USER:PASSWORD@HOST:3306/DATABASE
DATABASE_URL=

# 本地 API 端口（可选）。如果 3001 被占用，改成 3002/3003 等。
API_PORT=3001
